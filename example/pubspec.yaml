name: emv_card_reader_example
description: Demonstrates how to use the emv_card_reader plugin.

version: 0.0.1+0

publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  emv_card_reader:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
